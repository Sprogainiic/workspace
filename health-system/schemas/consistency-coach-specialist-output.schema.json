{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openclaw.local/health-system/schemas/consistency-coach-specialist-output.schema.json",
  "title": "Consistency Coach Specialist Output",
  "type": "object",
  "required": [
    "contract_version",
    "specialist",
    "request_id",
    "status",
    "summary",
    "recommendations",
    "risk_flags",
    "fallback",
    "modification_notes",
    "confidence",
    "handoff_note"
  ],
  "properties": {
    "contract_version": { "type": "string", "const": "specialist_contract_v1" },
    "specialist": { "type": "string", "const": "consistency_coach" },
    "request_id": { "type": "string", "minLength": 1 },
    "status": { "type": "string", "enum": ["ok", "needs_clarification", "unsafe_to_recommend"] },
    "summary": { "type": "string", "minLength": 1 },
    "recommendations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["type", "rationale", "payload"],
        "properties": {
          "type": { "type": "string", "enum": ["behavior_intervention", "reentry_plan", "friction_reduction", "risk_flag"] },
          "rationale": { "type": "string", "minLength": 1 },
          "payload": { "$ref": "consistency-coach-output.schema.json" }
        },
        "additionalProperties": false
      }
    },
    "risk_flags": { "type": "array", "items": { "type": "string" } },
    "fallback": {
      "type": "object",
      "required": ["MINIMUM_ACTION"],
      "properties": {
        "MINIMUM_ACTION": { "type": "string", "minLength": 5 }
      },
      "additionalProperties": true
    },
    "modification_notes": { "type": "array", "items": { "type": "string" } },
    "confidence": { "type": "string", "enum": ["low", "medium", "high"] },
    "handoff_note": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": false
}