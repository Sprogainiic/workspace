{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openclaw.local/health-system/schemas/personal-chef-specialist-output.schema.json",
  "title": "Personal Chef Specialist Output",
  "type": "object",
  "required": [
    "contract_version",
    "specialist",
    "request_id",
    "status",
    "summary",
    "recommendations",
    "risk_flags",
    "fallback",
    "modification_notes",
    "confidence",
    "handoff_note"
  ],
  "properties": {
    "contract_version": { "type": "string", "const": "specialist_contract_v1" },
    "specialist": { "type": "string", "const": "personal_chef" },
    "request_id": { "type": "string", "minLength": 1 },
    "status": { "type": "string", "enum": ["ok", "needs_clarification", "unsafe_to_recommend"] },
    "summary": { "type": "string", "minLength": 1 },
    "recommendations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["type", "rationale", "payload"],
        "properties": {
          "type": { "type": "string", "enum": ["meal_options", "fallback_meal", "simplicity_adjustment"] },
          "rationale": { "type": "string", "minLength": 1 },
          "payload": { "$ref": "personal-chef-output.schema.json" }
        },
        "additionalProperties": false
      }
    },
    "risk_flags": { "type": "array", "items": { "type": "string" } },
    "fallback": {
      "type": "object",
      "required": ["FALLBACK_MEAL"],
      "properties": {
        "FALLBACK_MEAL": { "type": "string", "minLength": 3 }
      },
      "additionalProperties": true
    },
    "modification_notes": { "type": "array", "items": { "type": "string" } },
    "confidence": { "type": "string", "enum": ["low", "medium", "high"] },
    "handoff_note": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": false
}